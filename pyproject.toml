[project]
name = "practice-voice-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "livekit>=1.1.7",
    "livekit-agents[openai,silero]~=1.5",
    "python-dotenv>=1.0.1",
]
